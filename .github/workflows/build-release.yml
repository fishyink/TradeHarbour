name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g., v1.6.1)'
        required: true
        default: 'v1.6.1'

permissions:
  contents: write
  actions: read
  security-events: write

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        include:
          - os: windows-latest
            platform: win
            artifact: TradeHarbour-${{ github.event.inputs.version || github.ref_name }}-portable.zip
          - os: macos-latest
            platform: mac
            artifact: TradeHarbour-${{ github.event.inputs.version || github.ref_name }}-mac.dmg
          - os: ubuntu-latest
            platform: linux
            artifact: TradeHarbour-${{ github.event.inputs.version || github.ref_name }}-linux.AppImage

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build application
        run: npm run build

      - name: Build Windows portable
        if: matrix.os == 'windows-latest'
        run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build macOS DMG
        if: matrix.os == 'macos-latest'
        run: npm run dist:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          CSC_LINK: ""
          CSC_KEY_PASSWORD: ""

      - name: Build Linux AppImage
        if: matrix.os == 'ubuntu-latest'
        run: npm run dist:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifact
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: release/*.zip

      - name: Upload macOS artifact
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: release/*.dmg

      - name: Upload Linux artifact
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: release/*.AppImage

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    permissions:
      contents: write
      actions: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: List downloaded files
        run: |
          echo "Downloaded artifacts:"
          find . -name "*.zip" -o -name "*.dmg" -o -name "*.AppImage" | head -20

      - name: Create Release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          name: Trade Harbour ${{ github.event.inputs.version || github.ref_name }} - Multi-Platform Release
          body: |
            🚀 **Trade Harbour ${{ github.event.inputs.version || github.ref_name }} - Multi-Platform Release**

            ## 📦 **Downloads Available**
            - **Windows**: Portable ZIP package
            - **macOS**: DMG installer for Intel & Apple Silicon Macs
            - **Linux**: AppImage for all distributions

            ## 🎯 **What's New**
            - ✅ **Streamlined Bybit Integration**: Focus on reliable Bybit-only support
            - ✅ **Cross-Platform Support**: Windows, macOS, and Linux builds
            - ✅ **Portable Applications**: No installation required on any platform
            - ✅ **Universal macOS**: Works on both Intel and Apple Silicon Macs

            ## 🗂️ **Installation Instructions**

            ### Windows
            1. Download `TradeHarbour-*-portable.zip`
            2. Extract to any folder
            3. Run `TradeHarbour.exe`

            ### macOS
            1. Download `TradeHarbour-*-mac.dmg`
            2. Open the DMG file
            3. Drag Trade Harbour to Applications folder
            4. Run from Applications (may need to approve in Security & Privacy)

            ### Linux
            1. Download `TradeHarbour-*-linux.AppImage`
            2. Make executable: `chmod +x TradeHarbour-*.AppImage`
            3. Run directly: `./TradeHarbour-*.AppImage`

            ## 📋 **Requirements**
            - **Windows**: Windows 10/11 (64-bit)
            - **macOS**: macOS 10.15+ (Intel & Apple Silicon)
            - **Linux**: Most modern distributions (64-bit)

            ---
            *Built with GitHub Actions for maximum compatibility across all platforms.*
          files: |
            windows-build/*.zip
            macos-build/*.dmg
            linux-build/*.AppImage
          draft: false
          prerelease: false